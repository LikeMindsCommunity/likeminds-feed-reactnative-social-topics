import {LMFeedCreatePollScreen} from '@likeminds.community/feed-rn-core';
import React from 'react';

const CreatePollScreen = () => {
  return <LMFeedCreatePollScreen />;
};

export default CreatePollScreen;
